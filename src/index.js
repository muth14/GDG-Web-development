import React from 'react';
import ReactDOM from 'react-dom/client';  // change here
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));  // create a root
root.render(<App />);  // render your app inside this root
